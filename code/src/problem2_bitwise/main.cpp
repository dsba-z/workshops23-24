#include <iostream>

int getBitAtIndex(int number, int idx)
{
    int bit = 0;
    return bit;
}


int countOnes(int number)
{
    int count = 0;
    return count;
}

void printBinary(int number)
{
    
}

int main()
{
    int number = 9;
    int idx = 2;
    getBitAtIndex(number, idx);
    countOnes(number);
    printBinary(number);
    return 0;
}
